

def breadth():


"""
1:1
2:1
2:2
2:3
2:4
3:1
3:2
"""


#Store Initial State
anodes.append(Node(depth,width,gridCopy(grid)))

    for depth
      width=1

      for elem cars #move every single car
      #every single direction
            up(anodes[depth-1][width])

              if == "Done":
                  #Check to see if matrix matches any nodes in closed list
                  for elem in anodes
                    if elem.matrix == currentMatrix:
                        return#do nothing?   keep going?
                #if not
                    else:

                """Create Node""" (depth+1, width)
                closedList.append("""currentNode""")






            #Update screen with vitals
            pgame(grid)
            print "Number of states created",len(anodes)
            print "Length of path", depth
            print "Open List length", len(openList)
            print "Closed List length", len(closedList)

                if evaluate=="WON":
                    #then stop....   set depth range to current?
                else:
                    width+=1


            down
            left
            right

                if evaluate(currentGrid)=="WON"
                    goal=width

def depth():


"""
1:1
2:1
3:1
4:1
5:1
2:2
3:2
4:2
